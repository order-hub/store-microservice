spring:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true

  datasource:
    url: jdbc:mysql://localhost:3306/orderHub
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: cassidy
    password: 65
